# Copy to .env.production for production builds (optional;
# `npm run deploy:frontend` already forces this URL).
VITE_API_URL=https://tallypns-api.onrender.com/api
# Set to true to enable 3D effects in production
VITE_ENABLE_3D=false
