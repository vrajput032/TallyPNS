/** Company GST identity used for returns / invoices (keep in sync with frontend). */
export const COMPANY = {
  name: "PNS ENTERPRISES",
  gstin: "06ABJFP8733H1ZW",
  /** First 2 digits of GSTIN = state code (Haryana = 06) */
  stateCode: "06",
} as const;
